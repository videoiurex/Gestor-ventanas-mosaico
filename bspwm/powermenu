#! /bin/sh

chosen=$(printf "Apagar\nReiniciar\nCerrarSesión" | rofi -dmenu -i)

case "$chosen" in
  "Apagar") systemctl poweroff ;;
  "Reiniciar") systemctl reboot ;;
  "CerrarSesión") i3-msg exit ;;
  *) exit 1 ;;
esac

